name: bold_portfolio
description: "BOLD Precious Metals Portfolio App"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
 
version: 1.0.0+1

environment:
  sdk: ^3.8.1

 
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_html: ^3.0.0  
  flutter_html_table: ^3.0.0 # Required for tables
  # flutter_html_iframe: ^3.0.0 # Required for YouTube/iFrames
  http: ^1.1.0
  shared_preferences: ^2.2.2
  fl_chart: ^0.66.0
  flutter_inappwebview: ^6.1.5
  flutter_native_splash: ^2.3.10
  provider: ^6.1.1
  syncfusion_flutter_charts: ^30.2.4
  flutter_background_service_ios: ^5.0.3  # Update to this version or the latest available version
  # chart_engine: ^2.1.1
  # flutter_typeahead: ^5.2.0
  # flutter_keyboard_visibility: ^6.0.0
  fluttertoast: ^8.2.4 
  image_picker: ^1.1.2
  font_awesome_flutter: ^10.6.0
  # path: ^1.8.3  
  intl: ^0.19.0 
  webview_flutter: ^4.2.2  # Or the latest version
  url_launcher: ^6.2.6
  google_fonts: ^6.1.0 
  flutter_dotenv: ^5.0.2
  flutter_background_service: ^5.0.1
  # google_sign_in: ^6.1.5 
  # flutter_svg: ^1.0.0
  pdf: ^3.10.7
  printing: ^5.13.1
  path_provider: ^2.1.2
  permission_handler: ^11.3.0
  share_plus: ^7.0.0
  local_auth: ^3.0.0 
  package_info_plus: ^8.0.0


flutter_launcher_icons:
  android: true
  ios: true  
  image_path: "assets/images/bold_logo.png" 


dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  assets: 
   - assets/env/.env.stagging
   - assets/env/.env.production
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
 

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_native_splash:
  color: "#FFFFFF" 
  image: assets/images/bold_logo.png 
  android_12:
    color: "#FFFFFF"  
    image: assets/images/bold_logo.png 

